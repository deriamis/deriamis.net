---
import { PageDataType } from '@types';
import ObfuscatedData from './ObfuscatedData.astro';

interface Props {
  phone: string;
  text?: string;
}

const { phone, text } = Astro.props;
---

<ObfuscatedData data={phone} text={text} type={PageDataType.PHONE} />